/**
 * Returns 42
 * @customfunction ANSWER
 * @returns String to write.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function answer(): string[][] {
  return Array(100000).fill(Array(12).fill("test-result"));
}
